# macOS development

Mode A targets macOS with Node.js 22 and pnpm 11. It will inspect and author project files without Power BI Desktop, Report Builder, a tenant, or live Analysis Services. Run `pnpm install`, `pnpm setup:electron`, then `pnpm dev`. The explicit setup step downloads Electron's local application binary. Structural and schema checks on macOS do not prove Microsoft application rendering.
