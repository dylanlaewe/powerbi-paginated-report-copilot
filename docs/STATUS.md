# Status

Current milestone: `v0.0.1` repository and CI bootstrap — local acceptance and macOS launch smoke test pass; remote push, merge, and tag pending.

Implemented: secure Electron shell, React status UI, strict workspace configuration, runtime-validated domain seed, automated quality workflow, and truthful documentation baseline.

Not implemented: project discovery, fixtures, TMDL/PBIR inspection, planning, authoring, validation, backup/restore, history, RDL generation, and Windows adapters.

Next: run all acceptance checks, push and merge the bootstrap, then implement PBIP project discovery.
