# v0.4 fake-provider corpus acceptance

This matrix records deterministic test expectations for the four accepted
fixtures. It contains no live Anthropic output, report content, paths,
candidate IDs, XML, rows, queries, or credentials.

Every planned result is an existing `EditPlan` and remains
`mutationAuthorized: false` until the unchanged operation-review workflow
receives exact user confirmations. The unsupported chart scenario creates
neither review nor output.
