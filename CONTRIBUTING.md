# Contributing

Use a short-lived conventional branch, add tests and truthful documentation, and run `pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build`. Use Conventional Commits. Never contribute secrets, employer materials, proprietary data, generated dependency folders, or claims of unperformed rendering validation.
